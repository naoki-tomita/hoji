{
  "name": "omusubi",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^10.12.18",
    "ts-node": "^7.0.1",
    "typescript": "^3.2.4"
  },
  "scripts": {
    "build": "yarn tsc -d --rootDir ./src --outDir ./dist",
    "test": "ts-node ./tests"
  }
}
